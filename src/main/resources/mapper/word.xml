<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- positioning DAO, groupID + artifactID + DAO Location -->
<mapper namespace="com.test.find_word.repository.WordDao">

    <select id="getWord" resultType="com.test.find_word.model.WordModel">
        SELECT *
        FROM word_sch
    </select>

</mapper>